function checkForAi() {
  /* checks the current player and
   * checks if that player is an ai;
   * return: boolean
   */
  let player = checkPlayer();
  if (player.ai === false) {
    return
  }
  return;
}


